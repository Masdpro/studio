# --- Database (SQLite file — nothing to sign up for) ---
DATABASE_URL=dev.db

# --- NextAuth ---
# Generate one with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:9002

# --- Genkit / Google AI (used by the AI flows in src/ai) ---
GOOGLE_GENAI_API_KEY=
